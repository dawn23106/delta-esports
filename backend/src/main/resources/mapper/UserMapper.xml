<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.delta.mapper.UserMapper">

    <resultMap id="UserResult" type="com.delta.entity.User">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="role" column="role"/>
        <result property="balance" column="balance"/>
        <result property="isActive" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="UserResult">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByPhone" resultMap="UserResult">
        SELECT * FROM users WHERE phone = #{phone}
    </select>

    <insert id="insert" parameterType="com.delta.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (phone, password, nickname, role, balance, is_active, created_at, updated_at)
        VALUES (#{phone}, #{password}, #{nickname}, #{role}, 0, true, now(), now())
    </insert>

    <update id="updateProfile">
        UPDATE users SET nickname = #{nickname}, updated_at = now() WHERE id = #{id}
    </update>

    <update id="updateRole">
        UPDATE users SET role = #{role}, updated_at = now() WHERE id = #{id}
    </update>

    <select id="findBoosters" resultMap="UserResult">
        SELECT * FROM users WHERE role = 'booster' AND is_active = true
    </select>

    <select id="findAll" resultMap="UserResult">
        SELECT * FROM users ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT count(*) FROM users
    </select>

</mapper>
