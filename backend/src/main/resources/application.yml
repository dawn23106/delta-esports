server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/delta_helper
    username: root
    password: 231062
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

jwt:
  access-secret: delta-access-secret-change-in-production
  access-ttl: 15m
  refresh-secret: delta-refresh-secret-change-in-production
  refresh-ttl: 7d
